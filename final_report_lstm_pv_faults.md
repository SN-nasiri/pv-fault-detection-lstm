**خروجی فایل اصلی دیتاست شامل سه نوع دیتاست میباشد :**

- dt1_solar_and_meteorological_measurement.csv → شامل داده‌های هواشناسی و تابش خورشیدی (مطابق جدول ۱)
- dt2_electrical_production_inverter_1_with_faults.csv → شامل تولید الکتریکی و وضعیت خطا (مطابق جدول ۲ و ۳)
- dt3_electrical_production_inverter_2.csv → مشابه dt2 ولی احتمالاً بدون خطا (یا اینورتر دوم)

**تحلیل دقیق و واقعی بر اساس جدول‌ها:**

ویژگی‌ها (Features) — مطابق Table 2:

- Ia, Ig, Pg, Eg, Va, Vg, Fg: همه متغیرهای الکتریکی ثبت‌شده توسط اینورتر
- ستون Fault: مقدار هدف (Target) که مشخص می‌کند چه نوع خطایی در لحظه رخ داده (یا خطا وجود ندارد)
- ستون time: زمان نمونه‌برداری؛ برای مدل LSTM جهت حفظ ترتیب زمانی حیاتی است.

**مقادیر گمشده:**

- فقط حدود ۱۰ مقدار در هر ستون دارای نقص بودند، در مقیاس ۳ میلیون داده، قابل چشم‌پوشی‌ است.
- تصمیم به حذف کامل این ردیف‌ها با dropna() منطقی است و از ورود خطا به مدل جلوگیری می‌کند.

کلاس‌های Fault — مطابق Table 3 :

| کلاس | توضیح | تعداد |
| --- | --- | --- |
| 0.0 | بدون خطا (No-fault) | 2,904,289 (~97%) |
| 1.0 | سایه یکنواخت | 6,318 |
| 2.1 – 2.3 | سایه جزئی دائم - کل پنل | مجموعاً ~7,900 |
| 3.1 – 3.2 | سایه جزئی دائم - بخشی از پنل | مجموعاً ~9,000 |
| 4.0 | سایه متناوب ایستا | 61,297 |

نکته مهم: داده‌ها به‌شدت نامتوازن هستند (کلاس 0 بسیار غالب است). این موضوع در آموزش مدل LSTM باید حتماً در نظر گرفته شود (مثلاً با class weighting یا sampling هوشمندانه).

**حذف داده‌های پرت (Outliers) و مقیاس‌بندی ویژگی‌ها**

1. شناسایی داده‌های پرت با استفاده از Z-Score:

- برای هر ویژگی عددی در ستون‌های 'Ia', 'Ig', 'Eg', 'Fg', 'Pg', 'Va', 'Vg'
- اگر Z-Score یک نمونه در هرکدام از این ویژگی‌ها بیش از ۵ باشد، آن نمونه به‌عنوان داده پرت حذف شده است.
- این مقدار ۵ نسبتاً محافظه‌کارانه است و فقط نقاط خیلی پرت رو حذف می‌کنه (که برای سیستم‌های صنعتی منطقیه)

1. مقیاس‌بندی ویژگی‌ها با StandardScaler:

- تمام ویژگی‌ها به توزیع نرمال با میانگین صفر و انحراف معیار یک مقیاس‌بندی شده‌اند (برای LSTM ضروریه).
- این باعث می‌شه مدل دچار سوگیری نسبت به متغیرهایی با مقیاس بزرگ‌تر نشه.

1. شکل نهایی دیتاست:

- بعد از حذف داده‌های پرت: 2,984,023 نمونه باقی مانده.
- هر نمونه شامل ۷ ویژگی عددی نرمال‌سازی‌شده است.

🔹 حذف داده‌های پرت:

- با استفاده از آستانه Z-Score بالاتر از ۵، حدود ۴۷۷۵ نمونه پرت از داده‌ها حذف شد.
- داده‌های پرت اغلب ناشی از نویز سنسور، خطای مخابراتی یا رفتار غیرعادی سیستم هستند.
- حذف آن‌ها به پایداری و دقت مدل LSTM کمک می‌کند.

🔹 مقیاس‌بندی ویژگی‌ها:

- با StandardScaler از scikit-learn انجام شد.
- میانگین هر ویژگی = ۰ و انحراف معیار = ۱.
- برای یادگیری پایدارتر در LSTM و جلوگیری از تسلط مقیاس توان (مثلاً Pg) بر بقیه ضروری است.

**ساخت توالی‌های ورودی:**

- برای یادگیری وابستگی زمانی، از sliding window به طول ۳۰ استفاده شده.
- یعنی هر ورودی مدل شامل ۳۰ گام زمانی (مثلاً ۳۰ بازه ۵ دقیقه‌ای یا ۳۰ ساعت، بسته به فرکانس داده) از ویژگی‌های نرمال‌شده است.
- برای هر توالی، برچسب هدف برابر با وضعیت سیستم در لحظه بعدی (t+30) قرار داده شده. این کار کمک می‌کند که مدل بتواند شرایط آینده نزدیک سیستم را پیش‌بینی کند.

🔹 خروجی:

- X_sequences.shape: شکل ورودی‌ها به مدل LSTM خواهد بود (نمونه‌ها, 30, 7)
- y_encoded: برچسب‌ها به صورت اعداد صحیح برای classification آماده شدند.

🔹 تقسیم‌بندی داده‌ها:

- ۷۰٪ برای آموزش (X_train, y_train)
- ۱۵٪ برای اعتبارسنجی (X_val, y_val)
- ۱۵٪ برای تست نهایی (X_test, y_test)
- این کار باعث می‌شه عملکرد مدل در شرایط دیده‌نشده ارزیابی بشه.

**undersampling برای متوازن‌سازی کلاس‌ها در مجموعه آموزش**

🔹 دلیل این کار:

- کلاس ۰ (بدون خطا) حدود ۹۷٪ داده‌ها را تشکیل می‌داد که باعث می‌شد مدل یاد بگیره «همیشه بگه سالم!»
- برای جلوگیری از bias toward majority class از undersampling استفاده شد.
- این روش به مدل اجازه می‌ده الگوهای مربوط به خطاها رو بهتر یاد بگیره.

🔹 روش به‌کاررفته:

- undersample کلاس غالب (کلاس ۰) به ۱۰٬۰۰۰ نمونه
- سایر کلاس‌ها به‌صورت کامل حفظ شدند (هیچ کاهشی برای minority classes اعمال نشد)
- روش بسیار مناسب برای شروع چون داده‌های خطا زیاد نیستند و نمی‌خوایم اونا رو دست‌کاری یا از دست بدیم.

🔹 نقاط قوت:

- باعث بهبود Recall برای کلاس‌های خطا خواهد شد.
- بسیار ساده و سریع اجرا می‌شه.
- مدل را از overfitting روی کلاس سالم نجات می‌ده.

**ساخت و آموزش مدل LSTM**

🔹 ساختار مدل:

- مدل شامل حدود ۳ لایه اصلی است.
- برای ورودی‌های (30, 7) طراحی شده.
- آخرین لایه با softmax خروجی احتمال هر کلاس خطا را تولید می‌کند.

🔹 تنظیمات آموزش:

- Optimizer: Adam (پیش‌فرض با learning rate خوب)
- Loss Function: sparse_categorical_crossentropy مناسب برای لیبل‌های عددی (نه one-hot)
- EarlyStopping: اگر مدل برای ۳ epoch متوالی پیشرفت نکنه، بهترین وزن ذخیره می‌شه.
- Batch size: 64 (مقدار متعادل برای training سرعت بالا و پایداری)

**ارزیابی مدل LSTM روی Validation متوازن‌شده**

خلاصه نتایج:

| معیار | مقدار |
| --- | --- |
| دقت کلی (Accuracy) | 0.98 (یا 98٪) |
| میانگین F1 ماکرو | 0.98 |
| کلاس غالب (C7 - احتمالاً کلاس 4.0) | بیشترین نمونه، دقت و Recall نزدیک به 100٪ |

گزارش classification_report:

| کلاس | Precision | Recall | F1-score | حمایت (Support) |
| --- | --- | --- | --- | --- |
| C0 (کلاس 0، بدون خطا) | 0.98 | 0.91 | 0.94 | 2000 |
| C1 تا C6 (انواع خطای سایه: 1.0 تا 3.2) | بین 0.97 تا 1.00 | بین 0.96 تا 1.00 | بین 0.97 تا 1.00 | 430 تا 1258 |
| C7 (کلاس 4.0 - intermittent shading) | 0.99 | 1.00 | 0.99 | 12,254 |

تمام کلاس‌ها عملکرد بسیار خوبی دارند، اما کلاس 0 (بدون خطا) کمی سخت‌تر توسط مدل تفکیک شده و بعضاً به اشتباه به‌عنوان خطا طبقه‌بندی شده (Recall = 91٪) که می‌تونه قابل‌درک باشه چون در دیتای training undersample شده بود.

**Confusion Matrix:**

توازن عالی بین Precision و Recall:

مدل هم در جلوگیری از آلارم‌های اشتباه (false alarms) خوب عمل کرده، و هم موارد واقعی خطا را با حساسیت بسیار بالا (high recall) تشخیص داده.

احتمال بسیار پایین Overfitting:

دقت validation بسیار بالا و یکنواخت بین کلاس‌هاست؛ Dropout و EarlyStopping به خوبی عمل کرده‌اند.

🔹 چرا کلاس 0 سخت‌تره؟

- در فرآیند undersampling فقط ۱۰٬۰۰۰ نمونه از کلاس ۰ وارد آموزش شد (در حالی که validation کلاس ۰ همچنان متنوع باقی‌موند).
- اما این trade-off پذیرفتنیه چون هدف مدل تشخیص دقیق خطاست، حتی اگر درصد کوچکی از سالم‌ها اشتباهاً هشدار بگیرند.

### **جمع‌بندی مرحله ارزیابی:**

مدل LSTM ساخته‌شده با ورودی دنباله‌ای از ۳۰ زمان‌گام از ۷ ویژگی، توانسته است:

- انواع سایه‌های دائم، متناوب، جزئی و یکنواخت را با دقت بسیار بالا تشخیص دهد.
- recall بالای ۰.۹۹ برای تمام کلاس‌های خطا کسب کند.
- در یک زمان آموزش نسبتاً کوتاه (احتمالاً زیر ۱۰ دقیقه) با EarlyStopping به همگرایی برسد.
- کاملاً مناسب استفاده عملی در مانیتورینگ نیروگاه PV باشد.

توزیع کلاس‌ها در تست:

| کلاس | تعداد نمونه | تفسیر |
| --- | --- | --- |
| 0   | 580,679 | بدون خطا (کلاس غالب) |
| 1   | 485 | Uniform shading |
| 2   | 430 | Constant partial shading (1 PV) |
| 3   | 492 | Constant partial shading (2 PV) |
| 4   | 657 | Constant partial shading (3 PV) |
| 5   | 543 | Partial shading – portion of module |
| 6   | 1259 | Partial shading – 2/3 module |
| 7   | 12,254 | Intermittent shading (بزرگ‌ترین کلاس خطا) |

- برای اطمینان از عمومیت (Generalization) مدل، تقسیم‌بندی نهایی روی کل داده اصلی (با توالی‌سازی) انجام شد.
- توزیع کلاس‌ها کاملاً نامتوازن است (بخصوص کلاس ۰ با بیش از ۹۷٪ نمونه‌ها)، که آزمون سخت‌تری برای مدل خواهد بود.
- این مجموعه تست به ما اجازه می‌دهد بررسی کنیم که مدل آموزش‌دیده روی داده balanced چطور روی داده‌های واقعی (نامتوازن) عمل می‌کند.

پیش‌بینی نهایی (Batch Inference)

| گام | توضیح |
| --- | --- |
| تعریف تابع batch_predict | تقسیم داده تست بزرگ به دسته‌های کوچک (batch_size=2048) برای جلوگیری از کمبود حافظه (Out of Memory) |
| استفاده از model.predict بدون verbose | پیش‌بینی بی‌صدا، سریع و بهینه |
| اتصال خروجی‌ها | با np.concatenate لیست خروجی‌ها به یک ماتریس بزرگ تبدیل می‌شود |
| نهایی‌سازی برچسب‌های پیش‌بینی‌شده | با np.argmax برای پیدا کردن کلاس نهایی (از خروجی softmax) |

اکنون متغیر y_pred_classes شامل برچسب‌های پیش‌بینی‌شده مدل روی تمامی 596,799 نمونه تست واقعی است.

**ارزیابی نهایی مدل روی داده تست اصلی (Stratified)**

خلاصه عددی از classification_report :

| کلاس (Class) | توضیح احتمالی | Precision | Recall | F1-score | Support |
| --- | --- | --- | --- | --- | --- |
| Class 0 | بدون خطا | 1.00 | 0.86 | 0.92 | 580,679 |
| Class 1 | Uniform shading | 0.42 | 0.98 | 0.58 | 485 |
| Class 2 | Partial shading (1 module) | 0.91 | 0.81 | 0.86 | 430 |
| Class 3 | Partial shading (2 modules) | 0.95 | 0.98 | 0.97 | 492 |
| Class 4 | Partial shading (3 modules) | 0.10 | 0.99 | 0.18 | 657 |
| Class 5 | Partial shading – portion | 0.71 | 0.92 | 0.80 | 543 |
| Class 6 | 2/3 module shaded | 0.19 | 0.97 | 0.31 | 1,259 |
| Class 7 | Intermittent shading | 0.15 | 0.99 | 0.25 | 12,254 |

نقاط قوت:

- مدل توانسته تقریباً همه‌ی خطاها را شناسایی کند (Recall بسیار بالا):  
    حتی کلاس‌های نادر مانند 1، 4، 6، 7 نیز recall نزدیک به ۱ دارند.
- Class 0 (سالم) با Precision فوق‌العاده 1.00 پیش‌بینی شده، یعنی هر زمان مدل گفته "سالمه" درست گفته، اما recall نسبتاً پایین‌تر شده چون نمونه‌های سالم زیادی به‌اشتباه به‌عنوان خطا شناسایی شده‌اند.
- مدل در تشخیص کلاس‌های بحرانی (مثل C3 و C5) دقت و توازن عالی بین precision و recall دارد.

نقاط قابل بهبود:

- Precision برای کلاس‌های سایه (C4، C6، C7) پایین است → مدل بعضی از موارد سالم را به‌اشتباه در این کلاس‌ها طبقه‌بندی می‌کند.
- این پایین بودن precision نشون می‌ده که model گاهی false positive زیادی برای برخی کلاس‌های خطا تولید می‌کنه.
- کلاس‌های 4 و 7 به‌دلیل اندازه بزرگ و تنوع بالا، طبقه‌بندی‌شون سخت‌تره (احتمال overlap ویژگی‌ها با کلاس ۰ بیشتره).

🔁 چرا این اتفاق افتاده؟

مدل روی داده balanced آموزش دیده (undersample‌شده)، اما حالا روی داده‌ی واقعی بسیار نامتوازن تست شده.

این باعث می‌شه مدل برای تشخیص خطاها بسیار حساس بشه (high recall) اما در نتیجه precision کاهش پیدا کنه.

این برای برخی کاربردها حتی desirable هست (مثلاً برای تشخیص اولیه خطا، بهتره هشدار زیاد بده تا اینکه خطا رو از دست بده).

جمع‌بندی نهایی

| معیار | نتیجه |
| --- | --- |
| دقت کلی (Accuracy) | 86٪ روی داده‌ی واقعی |
| Recall برای همه کلاس‌های خطا | بالای 97٪ ✅ |
| Precision پایین برای برخی کلاس‌ها | نشانه حساسیت زیاد مدل به خطاها (که برای fault detection خوبه) |
| Overfitting / Underfitting | مشاهده نمی‌شود؛ مدل generalize کرده اما biased toward خطاها است |
| نتیجه کلی | مدل به‌خوبی برای تشخیص سریع خطاها در سیستم واقعی PV مناسب است، حتی اگر false alarm داشته باشیم |

آموزش مدل LSTM روی کل داده (بدون undersampling)

ایجاد «داده ایمن» برای آموزش

| گام | شرح |
| --- | --- |
| Label Encoding | برچسب‌ها (y_labels) به عدد تبدیل شده تا برای Stratified Sampling و مدل‌سازی قابل استفاده باشند |
| Stratified Sampling (۵٪ داده) | با حفظ نسبت کلاس‌ها، ۵٪ داده‌ها انتخاب شدند |
| خروجی X_safe, y_safe | حجم پایین ولی نماینده از کل داده، برای آموزش ایمن و سریع |

X_safe: (149,199, 30, 7)

y_safe: (149,199,)

یعنی حدود ۱۵۰هزار نمونه دنباله‌ای با توزیع کلاس حفظ‌شده (stratified) داریم که بسیار مناسب برای:

- آزمایش سریع مدل‌ها
- جلوگیری از کرش یا مصرف بیش‌ازحد حافظه
- cross-validation‌ های سبک‌تر
- hyperparameter tuning اولیه

نکات :

| عنوان | توضیح |
| --- | --- |
| هدف | کاهش حجم داده برای آموزش امن (Safe Training Set) |
| روش | Stratified Shuffle Split باtest_size=0.95و random_state=42 |
| ویژگی مهم | حفظ کامل توزیع کلاس‌ها در نمونه کوچک |
| کاربرد | استفاده در آزمایش مدل‌های جدید یا سنگین (مثل Transformer)، آموزش سریع، یا نسخه دموی محصول |

تحلیل ساختار کلی :

انکود برچسب‌ها (LabelEncoder)

- تمام مقادیر y_safe به اعداد صحیح تبدیل شده‌اند.

تقسیم داده‌ها:

train/validation با نسبت 80/20

به صورت Stratified، یعنی نسبت کلاس‌ها حفظ شده (مهم برای یادگیری متوازن)

**ساخت مدل LSTM:**

| لایه | توضیح |
| --- | --- |
| LSTM(64) | یادگیری الگوهای زمانی با ۳۰ زمان‌گام و ۷ ویژگی |
| Dropout(0.3) | جلوگیری از overfitting |
| Dense(64, relu) | لایه fully connected برای استخراج ویژگی‌های غیرخطی |
| Dense(num_classes, softmax) | خروجی برای classification چندکلاسه (۸ کلاس) |

آموزش مدل:

- EarlyStopping با patience=3 روی val_loss → جلوگیری از بیش‌برازش
- batch_size=64 و تا 20 epoch
- ذخیره‌ی بهترین وزن‌ها به‌طور خودکار

ارزیابی روی Validation:

پیش‌بینی با model.predict

تبدیل softmax به کلاس نهایی با argmax

چاپ classification_report

رسم ماتریس سردرگمی (Confusion Matrix)

**تحلیل نتایج مدل LSTM آموزش‌دیده روی داده‌ی ایمن (Safe Set)**

خلاصه نتایج:

| متریک | مقدار |
| --- | --- |
| Accuracy کلی | 99% |
| Macro Avg F1-score | 0.75 |
| Weighted Avg F1-score | 0.99 |
| تعداد کل داده‌های Validation | 29,840 نمونه |

تحلیل بر اساس کلاس‌ها:

| کلاس | توضیح (طبق جدول داده‌ها) | F1-score | تحلیل |
| --- | --- | --- | --- |
| Class 0 | بدون خطا (No-fault) | 1.00 | مدل عملکرد عالی دارد؛ این کلاس اکثریت داده‌هاست. |
| Class 1 | سایه‌زنی یکنواخت | 0.91 | خیلی خوب؛ داده کم ولی مدل توانسته نسبتاً خوب یاد بگیرد. |
| Class 2 | سایه‌زنی ثابت - 1 ماژول | 0.65 | افت شدید دقت نسبت به کلاس 0؛ احتمالاً به دلیل کمبود نمونه و شباهت با Class 7. |
| Class 3 | سایه‌زنی ثابت - 2 ماژول | 0.87 | عملکرد بسیار خوب با داده‌ی کم. |
| Class 4 | سایه‌زنی ثابت - 3 ماژول | 0.42 ❗ | ضعیف‌ترین کلاس از نظر یادگیری، احتمالاً به‌خاطر کمبود نمونه یا هم‌پوشانی ویژگی‌ها با کلاس‌های مشابه. |
| Class 5 | سایه‌زنی جزئی - 1/3 ماژول | 0.62 | متوسط. احتمالاً نیاز به داده بیشتر دارد. |
| Class 6 | سایه‌زنی جزئی - 2/3 ماژول | 0.73 | قابل قبول. می‌تواند بهتر هم بشود. |
| Class 7 | سایه‌زنی متناوب (Intermittent static) | 0.81 | خوب، ولی با نرخ false positive نسبتاً بالا. |

نکات کلیدی از Confusion Matrix:

- کلاس‌های 1 تا 7 به‌خوبی از کلاس 0 جدا شده‌اند (کمترین اشتباه بین C0 و بقیه).
- بیشترین سردرگمی بین کلاس‌های سایه‌دار نزدیک به هم (مثلاً C2, C4, C7).
- عملکرد کلی مدل بسیار خوب است، به‌خصوص با توجه به این‌که روی داده‌های 5٪ اجرا شده.

نتیجه‌گیری عملی

| مورد | وضعیت |
| --- | --- |
| مدل | LSTM با 1 لایه 64 نرون |
| داده آموزشی | زیرمجموعه‌ای از داده‌های اصلی (5٪ Stratified) |
| داده اعتبارسنجی | 20٪ از Safe set |
| دقت کلی | 99% |
| عملکرد بر کلاس‌های نادر | نسبتاً خوب، ولی جای بهبود دارد (مثلاً C4 و C2) |
| نتیجه | مدل با داده کم هم توانسته الگوهای سایه‌زنی را تا حد خوبی تشخیص دهد. می‌توان از این برای نسخه سبک‌تر محصول یا آزمایشات سریع استفاده کرد. |

**تقسیم X_safe به Train/Test به‌صورت Stratified**

هدف:

ارزیابی نهایی مدل روی داده‌ای که در آموزش یا Validation استفاده نشده، ولی همچنان ایمن و تمیز است.

توزیع کلاس‌ها در y_test_strat:

| کلاس | تعداد نمونه | درصد نسبی (تقریبی) |
| --- | --- | --- |
| 0 (No Fault) | 29,034 | 97.3% |
| 1   | 24  | 0.08% |
| 2.1 | 21  | 0.07% |
| 2.2 | 25  | 0.08% |
| 2.3 | 33  | 0.11% |
| 3.1 | 27  | 0.09% |
| 3.2 | 63  | 0.21% |
| 4   | 613 | 2.05% |

همچنان Class 0 بسیار غالب است، اما ما توزیع یکنواخت‌تری نسبت به کل داده‌ها داریم (به کمک Stratified Split).

الان آماده‌ایم برای:

1. ارزیابی مدل آموزش‌دیده روی X_test_strat  
    برای بررسی عملکرد کلی مدل روی نمونه‌های ایمن و نادیده.
2. گزارش و confusion matrix نهایی از مدل روی test_strat  
    تا مطمئن شیم مدل فقط برای validation کار نکرده و generalize شده.

تحلیل نهایی عملکرد مدل روی داده Test ایمن (Stratified):

الان یک مدل LSTM پایدار، آموزش‌دیده و ارزیابی‌شده برای تشخیص انواع خطاهای ناشی از سایه در نیروگاه خورشیدی داریم — اون هم روی داده‌های پاک‌سازی‌شده، توالی‌دار، و تست‌شده به‌صورت stratified.

دقت کلی (Accuracy):

اما چون داده‌ها بسیار نامتوازن هستند، نگاه به averageها ضروریه:

| Metric | macro avg | weighted avg |
| --- | --- | --- |
| Precision | 0.80 | 0.99 |
| Recall | 0.68 | 0.99 |
| F1-score | 0.71 | 0.99 |

macro avg برای ما مهم‌تره چون برای کلاس‌های کوچک‌تر هم وزن برابری قائل میشه.

بررسی کلاس‌ها (کد کلاس بر اساس جدول Fault):

| کلاس | نوع خطا (جدول) | F1-score | توضیح |
| --- | --- | --- | --- |
| 0   | No Fault | 1.00 | عالی |
| 1   | Uniform Shading | 0.94 | پایدار |
| 2   | Constant shading - 1 module (2.1) | 0.38 | Recall ضعیف |
| 3   | Constant shading - 2 modules (2.2) | 0.77 | خوب |
| 4   | Constant shading - 3 modules (2.3) | 0.60 | متوسط |
| 5   | Constant shading (3.1) | 0.50 | بهبود نیاز دارد |
| 6   | Constant shading (3.2) | 0.65 | نسبتاً قابل قبول |
| 7   | Intermittent partial shading (4.0) | 0.81 | خیلی خوب |

نکات مثبت:

- مدل به‌خوبی کلاس 0 (no-fault) رو تشخیص می‌ده.
- Class 1 و Class 3 و Class 7 هم با دقت بالا تشخیص داده شدن.
- دقت بالا حتی روی داده‌های دیده‌نشده (test_strat).

نکات قابل بهبود:

Class 2 (شامل فقط 21 نمونه) با Recall = 0.24 عملکرد ضعیفی داره.

مدل در تشخیص برخی کلاس‌های جزئی‌تر مثل 5 یا 6 هنوز کمی مشکل داره (که طبیعی‌ست چون داده کمه).

جمع‌بندی کلی

مدل LSTM آموزش‌دیده با استفاده از داده‌های تمیز و متوازن‌شده توانسته عملکرد بسیار خوبی در تشخیص شرایط بدون خطا و بسیاری از حالت‌های مختلف سایه‌زنی از خود نشان دهد. با وجود توزیع نابرابر کلاس‌ها، استفاده از داده‌ی ایمن و ارزیابی دقیق نشان می‌دهد که مدل برای استفاده در محیط‌های واقعی نیز پتانسیل دارد.
